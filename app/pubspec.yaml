name: agenttalk
description: AI 카톡형 도트 컴패니언 (프로토타입)
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    # 노아 아바타(정적 이미지). 표정 바뀌는 애니메이션은 추후 도트 프레임으로 교체.
    - assets/character/noa.jpg
